import { SiteLS } from "../../../common/types/users";
import { connectedUserDefault } from "./users";

export const siteLSDefault: SiteLS = {
  connectedUser: connectedUserDefault,
  nbOpenedConnectedTabs: 0,
}
